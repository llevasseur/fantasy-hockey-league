/**
 * Displayed in the CountdownBox, the next player in the ranked queue or the User's favourites
 * @returns
 */
const PlayerNext = () => {
  return <div>PlayerNext</div>;
};

export default PlayerNext;
