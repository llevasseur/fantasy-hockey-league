/**
 * A card that displays essential player information (name, team, stats). Button's to add to favourites or to Draft
 * Displays when a user clicks on their name in the table
 * @returns
 */
const PlayerCard = () => {
  return <div>PlayerCard</div>;
};

export default PlayerCard;
