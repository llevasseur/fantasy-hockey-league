/**
 * A label paied with an Input, used in forms for data entry
 */
import Input from "../../atoms/Input/Input";

const FormField = () => {
  return (
    <label>
      <Input />
    </label>
  );
};

export default FormField;
