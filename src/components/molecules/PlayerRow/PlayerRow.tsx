/**
 * A player listed as a row detailing their name, team, stats. Filterable.
 * Consider this a more condensed version of PlayerCard
 * @returns
 */
const PlayerRow = () => {
  return <div>PlayerRow</div>;
};

export default PlayerRow;
