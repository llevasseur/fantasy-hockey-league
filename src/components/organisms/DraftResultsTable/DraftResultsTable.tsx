/**
 * A detailed table view of was picked when in each round.
 * Pick number, Player, Country, UserTeam, Rank
 * @returns
 */
const DraftResultsTable = () => {
  return <div>DraftResultsTable</div>;
};

export default DraftResultsTable;
